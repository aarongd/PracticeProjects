def add_2_list():
    li = []
    while True:
        num = input('enter number: ')
        if num == 'done':
            print(li)
            break
        if num
        num = int(num)
        if isinstance(num, int):
            li.append(num)

add_2_list()
